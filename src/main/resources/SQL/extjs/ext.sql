CREATE TABLE GRID_VIEW_CONFIGURE (
  ID      VARCHAR(255)  PRIMARY KEY NOT NULL,
  MODULE_NAME VARCHAR2(255) UNIQUE NOT NULL
);

CREATE TABLE EXT_COULUMN (
  ID VARCHAR(255) PRIMARY KEY NOT NULL,
  GRID_ID VARCHAR2(255) NOT NULL,
  HEADER VARCHAR2(255) DEFAULT '默认' NOT NULL,
  DATA_INDEX  VARCHAR2(255) NOT NULL
);
